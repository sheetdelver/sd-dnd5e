'use client';

import React from 'react';

/**
 * Extras tab (mobile view) — catch-all tab for additional content.
 *
 * STUB — static placeholder, no data wiring.
 */
export default function Extras() {
    return (
        <div style={{ padding: 'var(--space-lg)' }}>
            <div className="stub-placeholder" style={{ minHeight: '120px' }}>
                No extras to display
            </div>
        </div>
    );
}

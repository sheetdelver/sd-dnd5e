'use client';

import React from 'react';

/**
 * Features block — generic feature list renderer.
 * Can be used to display class features, species traits, feats, and other features.
 *
 * STUB — static placeholder, no data wiring.
 * TODO: Accept a category prop (class-features, species-traits, feats, other)
 */
export default function Features() {
    return (
        <div className="block-card">
            <h2 className="block-heading">Features</h2>
            {/* STUB: Empty state placeholder */}
            <div className="stub-placeholder">
                No features to display
            </div>
        </div>
    );
}

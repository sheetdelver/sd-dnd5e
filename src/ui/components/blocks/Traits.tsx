'use client';

import React from 'react';

/**
 * Traits block — species/racial traits list.
 *
 * STUB — static placeholder, no data wiring.
 */
export default function Traits() {
    return (
        <div className="block-card">
            <h2 className="block-heading">Traits</h2>
            {/* STUB: Empty traits list */}
            <div className="stub-placeholder">
                No species traits to display
            </div>
        </div>
    );
}

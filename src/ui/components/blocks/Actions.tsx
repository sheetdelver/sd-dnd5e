'use client';

import React from 'react';

/**
 * Actions block — generic action list renderer.
 * Can be used to display attacks, actions, bonus actions, reactions, or other actions.
 *
 * STUB — static placeholder, no data wiring.
 * TODO: Accept a category prop (attack, action, bonus-action, reaction, other, limited-use)
 */
export default function Actions() {
    return (
        <div className="block-card">
            <h2 className="block-heading">Actions</h2>
            {/* STUB: Empty state placeholder */}
            <div className="stub-placeholder">
                No actions to display
            </div>
        </div>
    );
}
